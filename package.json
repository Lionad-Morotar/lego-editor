{
  "name": "root",
  "private": true,
  "scripts": {
    "preinstall": "npm install -g lerna",
    "install": "lerna bootstrap --hoist",
    "start": "lerna run dev --stream --scope=wox-editor"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
